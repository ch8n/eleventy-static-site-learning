<ul>
    {% for post in posts %}
        <li>
            <strong>
                <a href = "{{post.url | url}}">{{post.data.title}}</a>
            </strong>
        </li>
        -
        <span>
            {{post.data.description}}
        </span>
    {% endfor %}
</ul>